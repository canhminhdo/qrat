//// Created by CanhDo on 2024/08/20.//#include "getch.h"#include <cstdio>using namespace std;string qrat::buf = "";int qrat::getch(void) { /* get a (possibly pushed back) character */    char c;    if (buf.size() > 0) {        c = buf.back();        buf.pop_back();        return c;    }    c = getchar();    return c;}void qrat::ungetch(int c) { /* push character back on input */    buf.push_back(c);}